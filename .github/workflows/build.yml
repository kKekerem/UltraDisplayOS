name: UltraDisplayOS Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  windows-build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Setup CMake
        uses: lukka/get-cmake@latest

      - name: Configure
        # Uses the Visual Studio 2022 generator as specified in CMakePresets.json
        run: cmake --preset windows-default

      - name: Build
        # Must use the build preset (windows-release) instead of the configure preset
        run: cmake --build --preset windows-release

      - name: Upload Windows Executable
        uses: actions/upload-artifact@v4
        with:
          name: ultradisplay-server-windows
          path: build/server/ultradisplay_server.exe

  linux-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            cmake \
            ninja-build \
            pkg-config \
            libdrm-dev \
            libva-dev \
            libasound2-dev \
            libevdev-dev \
            liburing-dev \
            libopus-dev

      - name: Configure
        run: cmake --preset linux-default

      - name: Build
        # Must use the build preset (linux-release) instead of the configure preset
        run: cmake --build --preset linux-release

      - name: Upload Linux Executable
        uses: actions/upload-artifact@v4
        with:
          name: ultradisplay-client-linux
          # Ninja generator places targets in their subdirectory folders under build
          path: build/client/ultradisplay_client

  buildroot-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Buildroot dependencies
        run: |
          sudo apt update
          sudo apt install -y sed make binutils build-essential gcc g++ bash patch \
            gzip bzip2 perl tar cpio unzip rsync file bc wget python3 cvs git \
            mercurial subversion xorriso grub2-common grub-pc-bin mtools dosfstools

      - name: Download Buildroot
        run: |
          wget https://buildroot.org/downloads/buildroot-2023.11.1.tar.gz
          tar -xvf buildroot-2023.11.1.tar.gz
          mv buildroot-2023.11.1 buildroot-src

      - name: Configure OS Image
        run: |
          # Copy custom configuration
          cp buildroot/buildroot.config buildroot-src/.config
          
          # Copy board files
          mkdir -p buildroot-src/board/ultradisplay
          cp -r buildroot/board/ultradisplay/* buildroot-src/board/ultradisplay/
          
          cd buildroot-src
          make olddefconfig

      - name: Build OS Image
        run: |
          cd buildroot-src
          # Warning: This compilation will take ~1-2 hours on standard GitHub Actions runners 
          # because it builds the entire GCC toolchain and Linux Kernel from scratch.
          make -j$(nproc)

      - name: Upload OS Images
        uses: actions/upload-artifact@v4
        with:
          name: ultradisplay-os-images
          path: |
            buildroot-src/output/images/ultradisplay-os.iso
            buildroot-src/output/images/sdcard.img
